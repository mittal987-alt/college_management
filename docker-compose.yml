version: '3.8'

services:
  college-assistant:
    build: .
    ports:
      - "8501:8501"

    volumes:
      - ./data:/app/data
      - ./.streamlit/secrets.toml:/app/.streamlit/secrets.toml:ro

    env_file:
      - .env

    restart: unless-stopped